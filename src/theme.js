import { createTheme } from '@mantine/core';

const theme = createTheme({
  white: '#fff',
  black: '#111',
  colors: {
    // TODO: Добавить больше цветов
    dark: ['#343434', '#21222c', '#171820', '#13151a'],
  },
});

export default theme;
